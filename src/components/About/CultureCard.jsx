
const CultureCard = ({ title, description }) => {
    return (
        <div className="culture-card">

            <h4>{title}</h4>

            <p>{description}</p>

        </div>
    );
};

export default CultureCard;